doctype html
html(lang="vi")
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title 404 — Trang không tìm thấy
    style.
      html,body{height:100%;margin:0;font-family:Arial,Helvetica,sans-serif;background:#071021;color:#e6eef6;display:flex;align-items:center;justify-content:center}
      .card{padding:32px;border-radius:12px;text-align:center;background:rgba(255,255,255,0.02)}
      .status{font-size:72px;color:#7dd3fc;margin:0}
      .btn{display:inline-block;margin-top:16px;padding:10px 16px;border-radius:8px;background:#7dd3fc;color:#071021;text-decoration:none}
  body
    .card
      h1.status 404
      h2 Trang không tìm thấy
      p Có thể URL sai hoặc trang đã bị xóa.
      a.btn(href="/") Về trang chủ
