mixin sub-menu(layoutProductCategory, products)
    ul 
        each item in layoutProductCategory 
            li
                a(href=`${products}/${item.slug}`) #{item.title}
                if item.children && item.children.length > 0 
                    +sub-menu(item.children, products)